Escopo:

Um site que faça o controle dos texto redigidos.

Tabelas:

CREATE TABLE USUARIO (
    id int NOT NULL PRIMARY KEY AUTO_INCREMENT,
    tipoDeUsuario int NOT NULL,
    nome varchar(100),
    telefone varchar(10),
    email varchar(100)
);

CREATE TABLE TAREFA (
    ID int NOT NULL PRIMARY KEY AUTO_INCREMENT,
    nome varchar(100),
    idCriador int NOT NULL FOREIGN KEY REFERENCES USUARIO(id),
    descricao text,
    dataLimite datetime,
    situacao int,
    valor decimal(10,2)
);


CREATE TABLE APLICACAO (
    ID int NOT NULL PRIMARY KEY AUTO_INCREMENT,
    idTarefa int NOT NULL FOREIGN KEY REFERENCES TAREFA(id),
    idUsuario int NOT NULL FOREIGN KEY REFERENCES USUARIO(id),
    texto text,
    dataDeAplicao datetime,
    observacoes text
);


CREATE TABLE COMENTARIO (
    ID int NOT NULL PRIMARY KEY AUTO_INCREMENT,
    sequencia int NOT NULL PRIMARY KEY,
    criador int NOT NULL FOREIGN KEY REFERENCES USUARIO(id),
    descricao text,
    dataPostagem datetime,
);

Funcionalidades:
Para empresas:
- Criar tarefas 
- Avaliar aplicação
Para usuarios (freelancer):
- Mandar aplicações

